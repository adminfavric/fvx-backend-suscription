# Django
Django==4.2.7
django-environ==0.11.2
django-model-utils==4.3.1

# Django REST Framework
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.0
drf-spectacular==0.26.5

# Database
psycopg2-binary==2.9.9
django-postgres-extra==2.0.8

# CORS
django-cors-headers==4.3.0

# Caching
redis==5.0.1
django-redis==5.4.0

# Image handling
Pillow==10.1.0

# Storage (pluggable backends: local FS by default; S3-compatible / GCS via env vars).
# Vienen instalados AMBOS SDKs (S3 + Google) para que cambiar de backend sea solo
# cuestión de env vars (STORAGE_BACKEND). GCS además requiere GS_BUCKET_NAME +
# GOOGLE_APPLICATION_CREDENTIALS (ver .env.example). Si quieres una imagen más
# liviana y NO usarás GCS, baja el extra a `django-storages[s3]`.
django-storages[s3,google]==1.14.2

# Utilities
python-dateutil==2.8.2
pytz==2023.3
google-auth==2.25.2
PyJWT==2.8.0
cryptography==42.0.0

# Data import / scraping
openpyxl==3.1.2
beautifulsoup4==4.12.2
requests==2.31.0

# Audit logging (optional but recommended)
django-auditlog==2.3.0

# Brute-force / credential-stuffing protection (lockout tras N fallos).
# Complementa el rate-limit (LoginIPRateThrottle/LoginUsernameRateThrottle):
# el throttle frena el ritmo, axes bloquea la cuenta por tiempo prolongado.
django-axes==6.4.0

# Celery + Beat — jobs async y tareas programadas. Broker = Redis (ya está).
# Una vez instalado, notifications.tasks.send_email_task se ejecuta async
# automáticamente (services.email._resolve_sync detecta la disponibilidad).
celery==5.3.6
django-celery-beat==2.5.0

# Sentry — error tracking + performance traces. Se activa solo si SENTRY_DSN
# está seteado (dev queda en blanco → no envía nada).
sentry-sdk[django]==2.8.0

# Email transaccional — ver docs/email.md
# (boto3 ya está arriba via django-storages[s3]; SES lo reusa).
jinja2==3.1.4              # render de templates email
mjml-python==1.4.0         # MJML → HTML responsive
premailer==3.10.0          # inline CSS para clientes legacy
# beautifulsoup4 ya está más abajo en este archivo (4.12.2) — sirve para html2text fallback

# Filtering and search
django-filter==23.3

# Development
ipython==8.17.2
django-extensions==3.2.3

# Testing
pytest==7.4.3
pytest-django==4.7.0
pytest-cov==4.1.0
factory-boy==3.3.0

# Code quality
black==23.11.0
flake8==6.1.0
isort==5.12.0

# Production server
gunicorn==21.2.0
whitenoise==6.6.0

# Environment
python-dotenv==1.0.0
